<div class="p-5 mb-4 bg-light rounded-3">
    <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold">Bem-vindo ao Blog do Node</h1>

        <p class="col-md-8 fs-4">This is a simple hero unit, a simple jumbotron-style component for calling extra
            attention to featured content or information.</p>

        <hr class="my-4">

        <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>

        <button class="btn btn-primary btn-lg" type="button">Crie uma conta</button>
    </div>
</div>

<hr>

<h2>Postagens recentes: </h2>

{{#each postagens}}
    <div class="card mb-4" >
        <div class="cardy-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
    </div>
</div>
{{else}}
    <h5>Nenhuma postagem</h5>
{{/each}}